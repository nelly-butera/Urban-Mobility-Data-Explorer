# =============================================================================
# .env.example  —  NYC Taxi Analytics Platform
# Commit THIS file to Git. Never commit the real .env file.
# To use: copy this file to .env and fill in your real values.
#   cp .env.example .env
# =============================================================================

# Neon PostgreSQL connection string (get from Neon dashboard)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Server
PORT=3000
HOST=0.0.0.0

# Data file paths (relative to project root)
TRIPS_CSV_PATH=./datasets/yellow_tripdata_2019-01.csv
ZONES_CSV_PATH=./datasets/taxi_zone_lookup.csv
SHAPEFILE_PATH=./datasets/taxi_zones/taxi_zones.shp
